import React from 'react';
import SearchSuggestionBox from './SearchSuggestionBox';

const SearchBar = () => {
  return (
    <div>
      SearchBar
      <SearchSuggestionBox />
    </div>
  );
};

export default SearchBar;
